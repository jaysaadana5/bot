def execute_real_trade(direction, amount):
    print(f"[REAL] Executing {direction} with ${amount}")
    # TODO: integrate Polymarket API here
    # Example:
    #   client = PolymarketClient(api_key=os.environ["POLYMARKET_API_KEY"])
    #   client.place_order(market="BTC-5MIN", side=direction, size=amount)
